<div class="rounded-md border-2 border-neutral-100 px-5 py-3 bg-neutral-50 h-fit">
  <%= link_to "＋ New room", new_room_path, data: { turbo_frame: "content" }, class: "mb-2 block text-neutral-900 font-medium" %>

  <div class="flex flex-col gap-y-1.5 tracking-tight">
    <% Room.order(:name).each do |room| %>
      <%= link_to "# #{room.name}", room_path(room), class: "text-neutral-900 #{"font-semibold" if @room == room}" %>
    <% end %>
  </div>
</div>
